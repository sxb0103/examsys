<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fqedu.examsys.dao.ShortSheetDao">
    <update id="updateShortSheet" parameterType="map">
       update short_sheet set score=#{score}
       where paperId=#{paperId} and userId=paperId=#{userId}
                                and short_id=paperId=#{shortId}
    </update>
</mapper>